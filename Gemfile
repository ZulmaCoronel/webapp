# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem "sinatra"  #Gema para aplicaciones web
gem "sinatra-contrib" #para no reiniciar a cada momento el servidor
gem "sqlite3"
gem "data_mapper"
gem "dm-sqlite-adapter"